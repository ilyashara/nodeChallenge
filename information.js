const myInfo = {
    name: "Ilyas",
    campus: "Toulouse",
}

const myInfos = () => {
message = `Hello, I'm ${myInfo.name}, I'm from ${myInfo.campus} campus` 
return message;
}

module.exports = myInfos;